[user]
%% switch on whoami
%% pmendes {
    name = pmendes
    email = pmendes@cloudflare.com
%% }
%% default {
    name = mendess
    email = pedro.mendes.26@gmail.com
%% }
%% end
[core]
    excludesfile = ~/.config/gitignore-global
    pager = delta
[pull]
    ff = only
[delta]
    plus-style = syntax "#012800"
    minus-color = normal "#340001"
    syntax-theme = base16
[interactive]
    diffFilter = delta --color-only
[init]
    defaultBranch = master
    templateDir = ~/.config/git/template
[diff]
    colorMoved = default
    algorithm = histogram # `patience` also a possible value
    context = 10
[merge]
%% switch on hostname
%% 3QWP3T3 {
    conflictstyle = diff3
%% }
%% default {
    conflictstyle = zdiff3
%% }
%% end
    tool = nvim
[commit]
    verbose = true
[rerere]
    enabled = true
[fetch]
    prune = true
    prunetags = true
[push]
    followtags = true

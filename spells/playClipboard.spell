#!/bin/bash

for clip in "$(xclip -sel clip -o)" "$(xclip -sel primary -o)"; do
    echo "Trying to play $clip"
    case "$clip" in
        *.png | *.jpg | *.jpeg)
            feh "$clip" && exit
            ;;
        *.gif)
            mpv --loop-file "$clip" && exit
            ;;
        *)
            mpv "$clip" && exit
            ;;
    esac
done

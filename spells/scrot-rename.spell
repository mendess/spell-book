#!/bin/bash

[ -n "$1" ] || exit 1

rename="$(true | dmenu -p 'Select picture name' | sed 's/^\s *//; s/\s*$//; s/\s\s*/_/g').png"

if [ -n "$rename" ]; then
    mv "$1" ~/Pictures/"$rename"
fi
notify-send 'Screenshot captured' "Saved to <b>~/Pictures/$rename</b>" -i "~/Pictures/$rename" -a scrot

#!/bin/bash

if git --version &>/dev/null
then
    git config --global user.name "mendess"
    git config --global user.email "pedro.mendes.26@gmail.com"
    git config --global core.excludesfile "$XDG_CONFIG_HOME"/gitignore-global
else
    echo Please install git
fi
